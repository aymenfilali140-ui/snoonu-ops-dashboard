// tailwind.config.ts
import type { Config } from "tailwindcss";

const config: Config = {
  // With Tailwind v4, you can keep this empty if you don't need custom theme yet.
  // This avoids extra complexity. We just let Tailwind use its defaults.
};

export default config;
